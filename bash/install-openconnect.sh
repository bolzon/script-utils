#!/bin/bash

sudo apt install git build-essential gettext autoconf \
  automake libproxy-dev libxml2-dev libtool vpnc-scripts \
  zlib1g-dev pkg-config libgnutls28-dev -y

# download last version (8+), build and install
